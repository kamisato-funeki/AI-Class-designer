# AI-Class-Designer 学生端 (Student App) 前端设计文档

## 1. 项目概述与技术栈选型

本项目为 `AI-Class-designer` 的配套学生端。由于需与教师端保持较高的一致性和开发效率，前端架构将继续沿用教师端的技术基础。

### 核心技术栈
- **核心框架**: Vue 3 (Composition API + `<script setup>` 语法糖)
- **开发构建**: Vite
- **编程语言**: TypeScript (严格模式，提供完整的接口类型推导)
- **路由管理**: Vue Router 4
- **状态管理**: Pinia
- **UI 组件库**: Ant Design Vue
- **网络请求**: Axios (封装请求拦截器、错误统一处理)
- **样式方案**: Tailwind CSS (可选) + CSS Variables + Scss/Less

---

## 2. 核心路由与功能模块划分

学生端主要侧重于“信息接收”、“内容消费”与“任务提交”。路由结构相对扁平：

| 路由路径 | 视图组件 (View) | 功能描述 |
| :--- | :--- | :--- |
| `/login` | [LoginView.vue](file:///f:/AI_class_designer/ai-class-designer/src/views/LoginView.vue) | 登录/注册页面 |
| `/dashboard` | `DashboardView.vue` | **学习看板**：今日课表、待办作业提醒、最新班级动态 |
| `/classes` | [ClassesView.vue](file:///f:/AI_class_designer/ai-class-designer/src/views/ClassesView.vue) | **我的班级**：已加入班级列表、输入邀请码加入新班 |
| `/classes/:id` | `ClassDetailView.vue` | **班级主页**：包含该班级的动态、资料、成员列表 |
| `/homework` | `HomeworkView.vue` | **作业中心**：全盘查看各科“待完成”、“已提交”、“已批改”作业 |
| `/homework/:id` | `HomeworkDetailView.vue`| **作业详情与提交**：查看作业要求、上传附件提交作业、查看评语 |
| `/courseware`| [CoursewareView.vue](file:///f:/AI_class_designer/ai-class-designer/src/views/CoursewareView.vue) | **课件学习**：浏览教师发布的课件资源、思维导图复习资料 |
| `/ai-tutor` | `AiTutorView.vue` | **AI 辅导**：基于本班级知识库 (RAG) 的专属 AI 答疑助手 |
| `/profile` | [ProfileView.vue](file:///f:/AI_class_designer/ai-class-designer/src/views/ProfileView.vue) | **个人中心**：个人信息修改、密码重置、系统设置 |

---

## 3. 前端目录结构设计 (src/)

建议采用基于特性的模块化（Feature-based）目录结构，便于长期维护：

```text
src/
├── api/                  # 接口统一管理
│   ├── auth.ts           # 登录注册相关 API
│   ├── class.ts          # 班级相关 API
│   ├── homework.ts       # 作业相关 API
│   └── courseware.ts     # 课件相关 API
├── assets/               # 静态资源 (图片、全局样式)
├── components/           # 全局公共组件
│   ├── layout/           # 页面布局组件 (Sidebar, Header)
│   ├── FileUploader/     # 作业通用上传组件
│   └── EmptyState/       # 通用空状态占位
├── hooks/                # 自定义组合式函数 (Composables)
├── router/               # 路由配置
├── store/                # Pinia 状态管理
│   ├── userStore.ts      # 用户状态
│   └── homeworkStore.ts  # 作业数据缓存状态
├── types/                # 全局 TypeScript 类型定义
│   ├── api.d.ts          # API 响应泛型
│   └── models.d.ts       # 核心业务实体类型
├── utils/                # 工具函数 (日期格式化、Axios 封装等)
└── views/                # 页面级视图组件
```

---

## 4. 核心接口类型定义 (TypeScript Interfaces)

我们在 `src/types/models.d.ts` 或具体的 `api/*.ts` 中预先定义好这些接口类型，保证前后端数据对接的严谨性。

### 4.1 基础网络请求响应泛型
```typescript
// src/types/api.d.ts
export interface ApiResponse<T = any> {
  code: number;       // 状态码，如 200 成功
  message: string;    // 提示信息
  data: T;            // 核心承载数据
}

export interface PaginatedData<T> {
  total: number;      // 总条数
  page: number;       // 当前页
  pageSize: number;   // 页大小
  list: T[];          // 分页数据列表
}
```

### 4.2 用户与班级实体 (User & Class)
```typescript
// 学生基础信息
export interface StudentInfo {
  id: string;
  studentNo: string;  // 学号
  name: string;
  avatar: string;
  email: string;
}

// 班级基础信息
export interface ClassInfo {
  id: string;
  name: string;
  teacherName: string;
  coverImage: string;
  joinCode: string;   // 加班邀请码
  studentCount: number;
}

// 课表详情实体
export interface ScheduleItem {
  id: string;
  classId: string;
  courseName: string;
  teacherName: string;
  dayOfWeek: number;  // 1-7 代表周一至周日
  startTime: string;  // "08:00"
  endTime: string;    // "08:45"
  roomPos?: string;   // 教室位置 (线下使用)
}
```

### 4.3 作业系统核心实体 (Homework)
*学生端作业系统的复杂度最高，包含查看【作业要求】和【自己的提交】。*

```typescript
// 附件实体 (题目附件或学生提交的附件)
export interface Attachment {
  id: string;
  fileName: string;
  fileUrl: string;
  fileType: string;
  fileSize: number;
}

// 教师发布的作业要求实体
export interface Homework {
  id: string;
  classId: string;
  title: string;
  description: string;
  attachments: Attachment[];
  publishTime: string;
  deadline: string;
  status: 'active' | 'expired'; // 当前作业是否已过期
}

// 学生的作业提交记录实体
export interface HomeworkSubmission {
  id: string;
  homeworkId: string;
  studentId: string;
  content: string;               // 文本回答
  attachments: Attachment[];     // 学生上传的作业附件
  status: 'unsubmitted' | 'submitted' | 'graded'; // 提交状态
  submitTime?: string;           // 提交时间
  
  // 教师打分与评语回显
  score?: number;                // 分数
  teacherFeedback?: string;      // 教师评语
  gradeTime?: string;            // 批改时间
}
```

### 4.4 课件与资讯实体 (Courseware & Dynamics)
```typescript
// 发布的课件资源实体
export interface Courseware {
  id: string;
  classId: string;
  title: string;
  coverImage: string;
  publishTime: string;
  type: 'slide' | 'document' | 'mindmap'; // 课件类型
  contentUrl: string; // 课件查阅或下载地址
}

// 班级动态/通知实体
export interface ClassDynamic {
  id: string;
  classId: string;
  publisherName: string; // 发布老师
  content: string;
  publishTime: string;
  isImportant: boolean;  // 是否置顶加急通知
}
```

---

## 5. 核心状态管理 (Pinia Store) 设计

### 5.1 用户状态 (`userStore.ts`)
负责管理学生登录状态、基础信息以及角色校验。

```typescript
import { defineStore } from 'pinia';
import { ref } from 'vue';
import type { StudentInfo } from '@/types/models';

export const useUserStore = defineStore('user', () => {
  const token = ref<string | null>(localStorage.getItem('token') || null);
  const userInfo = ref<StudentInfo | null>(null);

  const setToken = (newToken: string) => {
    token.value = newToken;
    localStorage.setItem('token', newToken);
  };

  const clearAuth = () => {
    token.value = null;
    userInfo.value = null;
    localStorage.removeItem('token');
  };

  return { token, userInfo, setToken, clearAuth };
});
```

### 5.2 作业状态 (`homeworkStore.ts`)
负责管理全局“待办作业”红点提醒和列表缓存。

```typescript
import { defineStore } from 'pinia';
import { ref, computed } from 'vue';
import type { HomeworkSubmission } from '@/types/models';

export const useHomeworkStore = defineStore('homework', () => {
  // 缓存学生的个人作业提交记录列表
  const submissions = ref<HomeworkSubmission[]>([]);

  // 计算属性：统计未提交的作业数量（用于侧边栏红点徽标提示）
  const unsubmittedCount = computed(() => {
    return submissions.value.filter(s => s.status === 'unsubmitted').length;
  });

  const setSubmissions = (data: HomeworkSubmission[]) => {
    submissions.value = data;
  };

  return { submissions, unsubmittedCount, setSubmissions };
});
```

---

## 6. 前端开发对接建议

1. **接口联调 (Mocking)**: 在后端 API 开发完成前，建议在前端项目内使用 `MSW` 或 `vite-plugin-mock`，依照上述定义的数据接口提前全量 Mock 数据，保证 UI 研发不受阻塞。
2. **组件复用**: 日期格式化组件、文件防抖上传组件、Pdf 预览组件强烈建议从现有的 `AI-Class-designer`（教师端）中抽离并在两个项目间共享，或者直接拷贝代码，以减少工作量。
3. **移动端适配**: 学生端可能会较高频地在手机/平板上查阅“待办作业”和“学习通知”，建议在布局时考虑使用响应式 (`grid/flex`)，或者在 CSS 端利用 Tailwind 预留的屏幕断点 (`sm:`, `md:`) 做好移动端兼容。
